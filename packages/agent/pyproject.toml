[project]
name = "table-extractor-agent"
version = "0.2.0"
description = "PDF table extraction tools using vision models"
requires-python = ">=3.13"
dependencies = [
    "google-genai>=1.0",
    "pdfplumber>=0.11",
    "openpyxl>=3.1",
]
